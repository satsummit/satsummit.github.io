import React  from 'react';

import Layout from '../components/layout';

const IndexPage = (props) => {
  return (
    <Layout>
      <main>Hello Satsummit</main>
    </Layout>
  );
};

export default IndexPage;
